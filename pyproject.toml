[tool.poetry]
name = "poetry env"
version = "0.1.0"
description = ""
authors = ["Solveyga <azbite@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pygame = "^2.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
